name: Deploy Angular to GitHub Pages

on:
  push:
    branches:
      - main # Triggers when you push to the main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: |
          cd angular-app
          npm install

      - name: Build Application
        run: |
          cd angular-app
          npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: angular-app/dist/angular-app # The built files location
          branch: gh-pages # The branch GitHub Pages will deploy from


# To activate deployment for React:

# name: Deploy React to GitHub Pages

# on:
#   push:
#     branches: [ main ]
#     paths: [ 'react-app/**' ] # Triggers only when React code changes

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 18

#       - name: Install & Build
#         run: |
#           cd react-app
#           npm install
#           npm run build

#       - name: Deploy
#         uses: JamesIves/github-pages-deploy-action@v4
#         with:
#           folder: react-app/build
#           branch: gh-pages-react # Custom separate branch for React

